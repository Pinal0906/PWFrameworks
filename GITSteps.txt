1. download git and install
2. then write command git init , you will gt status of all the files
    -- U - means untracked files, means they are ready to track or to staging
    -- A - means index added or its already there on global repo git
3. then write git config --global user.name "pinal0906" - needs 1 time only
4. then write git config --global user.email "pbpandya96@gmail.com" - needs 1 time only
5. git add -a = all files will be added to global repositery
- similarly we have other options to add
   --if you want to add only single file then git add file name
   --if files with only java extension then git add *.java
   --if all file wit folder git add foldername
   --if everything then git add .
6. Then write git commit -m "any message"
7. Then after all these my all the files on my local reporsitory from my system

so, first we created project, then we tracked those files using git init 
then we added indexed using git add -a and added files to staging area then
we commited to our local repositorey of git using git commit - "msg" command
then u wont see any U or A on project which is commited

8. write command git status - by default it will be on master branch we have not selected branch at the time of commit

9. Create a repositorey into your git using New button, once u create u will get once
url, which is very imp

10 Now we need to connect our local repositorey to remote repositorey one time
for this use command git remote add origin https://github.com/Pinal0906/PWFramework.git
this is the same url we got in step 9

11. Now final step : push these files to remote repositorey
so use command git push origin master
once u push u will get authentication - provide authentication credentials using tokem or browser authentication

**********************************************************************************
---Now process of adding remote file created from github to local repositorey------------

1. add file from your remote repo using Addfiles button inside your onr of the project
goto your icon from corner > select repositories option > select you project > click on Add file
once you fill evrything in ur file > click on Commit Changes button > add message and again click commit changes button

2. Now type command in terminal git fetch origin master OR git pull origin master
FETCH - comand will fetch only the new file added on project
PULL - this command will pull eveything of this project

3. Now if I modify any files then after modifiyng write a command
1.git add file name
2.git commit -m "message"
3. git push origin master.

*******************************************************************************************

Now following is the way to get a clone of any new project from remote to local
1. type command git clone <repositorey url>

****************************************************************************
Switching branchs  
1. if we want to switch to any other branch then use command
git branch branchname //creates a new branch
git checkout branch name // switch to branch
git branch --show-current // to check current branch

similarly like project we can push newly created branch to remote as well
git add .
git commit -m "message"
git push branchname

and then when you goto github then you will see button "Compare & pull request"
click on it > click on Pull request button
then click on "Merge Pull Request" button
then code reviewer will review your command and do the change according to feedback
then click on Confirm Merge button
